# Comment Copilot — Web

Next.js 16 后端 + Web 控制台，详见根目录 [README](../README.md)。

## 快速启动

```bash
npm install
cp .env.local.example .env.local   # 填写环境变量
npx dotenv-cli -e .env.local -- npx drizzle-kit migrate
npm run dev
```

访问 http://localhost:3000
