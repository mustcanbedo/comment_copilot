# Comment Copilot - 产品需求文档 (PRD)

> Comment Copilot 旨在让"评论区 = 销售漏斗上游"，通过 AI 赋能内容创作者实现 **评论互动辅助、潜客识别、私信转化** 的一体化工具体验。

最后更新：2026-03-12（v2.0 单人 MVP 版）

---

## 1. 产品概述

### 1.1 产品定位

- 目标用户：多平台带货创作者（小红书 / 抖音 / 快手）、私域运营团队。
- 场景痛点：评论量激增但人力有限、无法快速识别潜客、转化链路断裂。
- 价值主张：将评论区从"互动区"升级为"销售漏斗入口"，提升回复效率与潜客命中率。

### 1.2 愿景与差异化

| 维度 | Comment Copilot | 市面竞品差异 |
|---|---|---|
| 评论抓取 | 插件侧 DOM 解析，合规采集 | 多数竞品依赖 API，稳定性受限 |
| AI 能力 | 人设模版 + DeepSeek-V3 中文优化 | 通用大模型回复，缺少平台语境 |
| 私信转化 | 支持官方留资卡片、转私域策略 | 很少覆盖转化链路 |
| 数据复盘 | 潜客热力图 + 爆款内容解剖 | 仅提供评论数量级统计 |

---

## 2. 目标用户与场景

### 2.1 Persona 细分

| Persona | 日志量级 | 核心 KPI | 典型场景 |
|---|---|---|---|
| P1 单账号带货博主 | 200~1,000 条评论/日 | 回复率 ≥95%，日转化 GMV | 小红书/抖音短视频，个人运营 |
| P2 品牌私域运营 | 5-10 账号 | 新增线索/天，客服人效 | 多平台内容矩阵，需导流企业微信 |

### 2.2 JTBD & 场景机会

| JTBD | 场景描述 | 当前痛点 | 机会 |
|---|---|---|---|
| 我需要快速回复评论不丢失人设 | 视频上线后 2h 评论暴涨 | 纯人工+通用模版慢且风格割裂 | AI 人设模版 + SLA 提醒 |
| 我想把意向用户导入私信/私域 | 评论里有人问价/链接 | 难以识别高意向、话术不统一 | 意向打分 + 私信脚本 |

---

## 3. 问题陈述 & 成功指标

### 3.1 可衡量的核心问题

1. **回复 SLA 不可控**：人工回复速度 >30 分钟，平台权重下降。
2. **潜客识别靠经验**：无意图标签，运营无法优先处理高价值评论。
3. **私信转化链断裂**：缺少标准话术和留资入口，导致流量损失。

### 3.2 成功指标 & 目标

| 指标 | Baseline | 目标 (MVP) | 度量方式 |
|---|---|---|---|
| 评论回复率 | 60% | 90% | 插件侧统计 |
| 平均响应时间 | 25 min | <5 min | 采集评论到回复的时间差 |
| 私信转化率 | 8% | 15% | 生成私信脚本并发送后 48h 转化 |

北极星指标：**评论驱动 GMV**（来自评论互动的成交金额）。

里程碑：

- M0 (3 周)：极简 MVP — 小红书评论抓取 + AI 回复建议 + 手动发送
- M1 (8 周)：产品化 — Lead Scanner + 抖音支持 + 简版 UI
- M2 (12 周+)：数据驱动 — 由用户反馈决定后续功能

---

## 4. 产品模块设计

### 4.1 Comment Copilot（评论助手）— MVP 核心

- **用户故事**：作为博主，我希望在插件侧面板聚合评论并快速获得 AI 回复建议，手动确认后复制发送。
- **触发条件**：插件检测到页面评论区 DOM 变化（MutationObserver）。
- **输入**：评论内容、人设配置（语气、禁用词）。
- **AI 能力**：DeepSeek-V3 生成 3 条候选回复，用户选择后手动复制/发送。
- **输出**：3 条候选回复 + 一键复制。
- **容错**：若 AI 失败，展示历史模版供用户选择。

### 4.2 Lead Scanner（潜客识别）— M1

- **意向标签**：`hot`（高意向）、`warm`（中意向）、`cold`（普通互动）、`spam`（垃圾/风险）。
- **算法**：关键词规则优先（问价/链接/求购），辅以 DeepSeek 语义判断。
- **输出**：潜客列表、处理状态。

### 4.3 DM Convertor（私信转化）— M1

- **功能**：私信脚本生成（多轮引导、留资提示）、复制脚本至客服工具。
- **风控**：节流（≥30s / 条）、人工审核开关，**不自动发送**。

### 4.4 Viral Analyzer（爆款分析）— M2 及以后

- 根据 M1 用户反馈决定是否优先开发。

### 4.5 配置中心

- 人设模版库（语气、emoji、禁用词）。
- 敏感词 & 风控策略。
- SLA 模块：为不同账号设置响应目标，逾期提醒。

### 4.6 Onboarding 流程

**目标：用户在 30 分钟内完成首次评论回复。**

```
Step 1  安装插件
        Chrome 商店安装 → 弹出引导页 → 点击"开始接入"

Step 2  注册 / 登录
        邮箱注册 OR Google OAuth → 自动创建 tenant

Step 3  绑定平台账号
        在插件内打开小红书/抖音 → 插件检测登录态 → 一键绑定
        （无需 OAuth，读取浏览器已渲染的 DOM，不主动发起平台 HTTP 请求）

Step 4  填写人设关键词（简化版）
        填写 3-5 个关键词描述自己的风格 → AI 据此生成初版人设模板

Step 5  首次评论处理演示
        选取 1 条真实评论 → 展示 AI 回复建议 → 用户手动复制发送
        完成后标记 Onboarding 完成，进入正式使用
```

### 4.7 配额管理

| 套餐 | 月配额 | 超额 100% 时 |
|---|---|---|
| Basic | 10k 条 | 降级为仅处理 hot 高意向评论 |
| Pro | 60k 条 | 同上，保留人工审核通道 |
| Agency | 200k 条 | 按超额量计费，不中断服务 |

### 4.8 大促模式

**触发方式**：运营手动开启，设置持续时长（默认 24h）。

| 维度 | 正常模式 | 大促模式 |
|---|---|---|
| 优先队列 | 全意向类型 | 仅处理 hot 评论 |
| SLA 目标 | 2h 内处理 hot 评论 | 30min 内处理 hot 评论 |

---

## 5. 合规与风控

- **数据采集合规**：
  - 插件**仅解析浏览器已渲染的 DOM**，不主动发起任何对平台服务器的 HTTP 请求。
  - 所有数据获取通过 `MutationObserver` 监听 DOM 变化完成，定位为"用户数字助理"而非"爬虫"。
  - 插件在 Onboarding 时明确告知用户数据采集范围，需用户主动授权。
  - Cookie 数据仅在本地处理，不上传服务器。

- **AI 标识**：
  - 小红书、抖音平台**不允许**在公开评论中标注 AI 生成，默认**关闭**对外 AI 标识。
  - 系统内部对所有 AI 生成内容打标（`ai_replies.model_used` 字段），便于审计。

- **发送限制**：
  - **不自动发送评论**，需用户手动复制或确认。
  - 评论发送节流 ≥30 秒，私信节流 ≥60 秒。
  - 异常触发（账号被限流）时自动降级到人工模式并推送告警。

- **风险控制**：
  - 断路器机制：连续 5 次 DOM 选择器失败时停止所有操作，提示用户等待适配。
  - 随机延迟：模拟用户操作间加入 300~500ms 随机延迟，避免机器行为特征。

- **数据合规**：
  - 插件仅采集评论公开数据；敏感信息入库加密。
  - 日志保留 180 天，可导出/删除以满足 GDPR/中国网络安全要求。

---

## 6. 技术与依赖

- **采集层**：Chrome 插件（Plasmo + React + Tailwind），MutationObserver DOM 解析 + 去重。
- **Web 控制台**：Next.js App Router，部署于 Vercel。
- **后端 API**：Next.js Route Handlers（同一项目），无需独立后端服务。
- **数据库**：Neon DB（Serverless PostgreSQL），Drizzle ORM。
- **后台任务**：Inngest（无服务器函数队列，替代 BullMQ + Redis）。
- **AI 服务**：DeepSeek-V3（中文评论回复，主力）；GPT-4o（多语言/英文，备用）。
- **监控**：Sentry（错误追踪）+ Vercel Analytics（性能）。
- **API 密钥管理**：Vercel 环境变量（加密存储），无需 Vault/KMS。

---

## 7. MVP 范围（3 周交付）

### Week 1-2：核心链路

- 小红书评论 DOM 抓取（MutationObserver）。
- 后端 `/api/ingest/comments` 写入 Neon DB。
- 后端 `/api/ai/reply` 调用 DeepSeek-V3 返回 3 条建议。
- 插件 Sidebar：展示评论列表 + 生成回复 + 一键复制。

### Week 3：可用性

- 用户注册/登录（NextAuth.js）。
- 人设配置页面。
- 部署到 Vercel + Railway（或纯 Vercel）。
- 10 个种子用户测试。

---

## 8. 商业模式

| 套餐 | 价格 (USD/月) | 账号数 | 评论配额 | 关键权益 |
|---|---|---|---|---|
| Basic | $19 | 1 | 10k 条 | 评论聚合、AI 回复、基础模版 |
| Pro | $39 | 5 | 60k 条 | 潜客识别、私信脚本、周报 |
| Agency | $79 | 20 | 200k 条 | 多账号权限、白标报表、API |

附加策略：14 天免费试用（Basic 功能），转化期内重点推送成功案例。

---

## 9. 风险与未决问题

| 项目 | 描述 | 状态 | 缓解策略 |
|---|---|---|---|
| 平台政策 | 评论采集与自动回复合规性 | 需持续评估 | DOM 解析定位为"数字助理"，保留人工审核 |
| 模型成本 | AI 调用成本 | 中 | 默认 DeepSeek-V3，缓存模版，月度成本告警 |
| DOM 变更 | 平台前端改版导致抓取失败 | 高 | 断路器 + selector 热更新机制 |
| Chrome 权限 | 插件需读取评论 DOM | 中 | 明确权限说明，通过商店审核 |

---

本文档将随版本迭代持续更新，详见路线图章节。
