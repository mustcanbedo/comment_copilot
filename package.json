{
  "name": "comment-copilot-monorepo",
  "private": true,
  "scripts": {
    "dev": "cd apps/extension && npm run dev",
    "dev:prod": "cd apps/extension && npm run dev:prod",
    "dev:backend": "cd backend && go run ./cmd/server",
    "build": "cd apps/extension && npm run build"
  },
  "workspaces": [
    "apps/*",
    "packages/*"
  ]
}
